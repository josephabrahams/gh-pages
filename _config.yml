# Jekyll config

exclude: [
    bower.json,
    CNAME,
    Gemfile,
    Gemfile.lock,
    Gruntfile.js,
    LICENSE,
    node_modules,
    package.json,
    README.md,
    vendor
  ]

safe: true

sass:
  style: :compressed

